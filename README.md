# audio
:D

# Hot to solve compilation problem: error: failed to run custom build command for `alsa-sys v0.3.1`

Su Ubuntu, Debian o derivati:
sudo apt update
sudo apt install libasound2-dev pkg-config

Su Fedora:
sudo dnf install alsa-lib-devel pkgconf-pkg-config

Su Arch:
sudo pacman -S alsa-lib pkgconf


